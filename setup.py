"""Install script for the bio-clip package."""
from setuptools import find_packages, setup

setup(
    name="protein-structure-tokenizer",
    version="0.0.1",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)